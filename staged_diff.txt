diff --git a/requirements.txt b/requirements.txt
index b80870c..896ac95 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -1,8 +1,8 @@
-torch
-torchvision
-opencv-python
-numpy
-pyvirtualcam
-depthai
-timm
-pywin32; sys_platform == 'win32'
+torch>=2.0.0
+torchvision>=0.15.0
+opencv-python>=4.8.0.0
+numpy>=1.24.0
+pyvirtualcam>=0.11.0
+depthai>=2.24.0.0
+timm>=0.9.0
+pywin32>=306; sys_platform == 'win32'
diff --git a/vd.py b/vd.py
index 3305d39..9844b97 100644
--- a/vd.py
+++ b/vd.py
@@ -193,6 +193,7 @@ def depth_to_grayscale_oak(depth_mm: np.ndarray, params: DepthParams, state: dic
     else:
         n = int(params.manual_near_oak)
         f = int(params.manual_far_oak)
+        if f <= n: f = n + 1
 
     d = depth_mm.astype(np.float32)
     invalid = (d <= 0)
@@ -248,8 +249,8 @@ def parse_args() -> argparse.Namespace:
 
     ap.add_argument("--gamma", type=float, default=0.60)
     ap.add_argument("--median-ksize", type=int, default=5)
-    ap.add_argument("--no-flip-ud", action="store_true")
-    ap.add_argument("--no-flip-lr", action="store_true")
+    ap.add_argument("--flip-ud", action="store_true")
+    ap.add_argument("--flip-lr", action="store_true")
 
     ap.add_argument("--window", default="vOICe Depth (capture)")
     ap.add_argument("--window-w", type=int, default=960)
@@ -267,8 +268,8 @@ def main() -> int:
         auto_smooth=float(args.auto_smooth),
         gamma=float(args.gamma),
         median_ksize=int(args.median_ksize),
-        flip_ud=not args.no_flip_ud,
-        flip_lr=not args.no_flip_lr,
+        flip_ud=args.flip_ud,
+        flip_lr=args.flip_lr,
     )
 
     out_size = (int(args.width), int(args.height))
@@ -485,8 +486,8 @@ def main() -> int:
                 params.auto_range = True
                 params.gamma = 0.60
                 params.median_ksize = 5
-                params.flip_ud = not args.no_flip_ud
-                params.flip_lr = not args.no_flip_lr
+                params.flip_ud = args.flip_ud
+                params.flip_lr = args.flip_lr
                 gamma_idx = min(range(len(gamma_cycle)), key=lambda i: abs(gamma_cycle[i] - params.gamma))
                 state.clear()
                 tts_queue.put("Resetting parameters to defaults")
diff --git a/voice_depth_virtualcam.py b/voice_depth_virtualcam.py
index f805df3..a5356b9 100644
--- a/voice_depth_virtualcam.py
+++ b/voice_depth_virtualcam.py
@@ -179,6 +179,7 @@ def depth_to_grayscale_oak(depth_mm: np.ndarray, params: DepthParams, state: dic
     else:
         n = int(params.manual_near_oak)
         f = int(params.manual_far_oak)
+        if f <= n: f = n + 1
 
     d = depth_mm.astype(np.float32)
     invalid = (d <= 0)
@@ -223,8 +224,8 @@ def parse_args() -> argparse.Namespace:
     ap.add_argument("--no-invert", action="store_true")
     ap.add_argument("--no-auto-range", action="store_true")
 
-    ap.add_argument("--no-flip-ud", action="store_true")
-    ap.add_argument("--no-flip-lr", action="store_true")
+    ap.add_argument("--flip-ud", action="store_true")
+    ap.add_argument("--flip-lr", action="store_true")
 
     ap.add_argument("--no-preview", action="store_true", help="Do not show any OpenCV window (virtual cam only)")
     ap.add_argument("--window", default="vOICe Depth (no HUD)")
@@ -243,8 +244,8 @@ def main() -> int:
     params = DepthParams(
         invert=not args.no_invert,
         auto_range=not args.no_auto_range,
-        flip_ud=not args.no_flip_ud,
-        flip_lr=not args.no_flip_lr
+        flip_ud=args.flip_ud,
+        flip_lr=args.flip_lr
     )
     
     out_size = (int(args.width), int(args.height))
@@ -442,6 +443,13 @@ def main() -> int:
                 elif k == ord("h"):
                     params.flip_lr = not params.flip_lr
                     tts_queue.put(f"Flip left right {'ON' if params.flip_lr else 'OFF'}")
+                elif k == ord("r"):
+                    params.invert = True
+                    params.auto_range = True
+                    params.flip_ud = args.flip_ud
+                    params.flip_lr = args.flip_lr
+                    state.clear()
+                    tts_queue.put("Resetting parameters to defaults")
 
     finally:
         if 'tts_queue' in locals():
